RAG means retrieval augmented generation. It searches trusted documents before answering.
Vector search helps find related meaning, not exact keywords.
For this project, PostgreSQL with pgvector is preferred because it keeps metadata and vectors together.
